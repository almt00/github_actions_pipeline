pre-commit:
  parallel: true
  commands:
    linter:
      run: npm run lint
    format:
      run: npm run format
    tests:
      run: npm run test

pre-push:
  parallel: true
  commands:
    linter:
      run: npm run lint
    format:
      run: npm run format
    tests:
      run: npm run test
